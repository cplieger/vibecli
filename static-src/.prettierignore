.vitest-cache/
coverage/

# Stryker's sandbox + report output and the Kiro skill scratch trees. All are
# gitignored, but Prettier does not read .gitignore, so a leftover sandbox (an
# interrupted mutation run never cleans it up) or a review run makes
# `npm run lint:prettier` fail on ~100 copied files. vitest.config.ts excludes
# the same trees for the same reason.
.stryker-tmp/
reports/
.code-review/
